/* function jqueryCC() {
  // Hide preheader & move navbar when scroll
  $(window).scroll(function () {
    const windowScrollTop = $(this).scrollTop();

    if (windowScrollTop > 200 && $('.navbar_section').css('display') === 'block') {
      $('.navbar_section').css({
        transform: 'translateY(-7rem)',
      });
      $('.button.is-icon.is-navbar').css({
        transform: 'translateY(7rem)',
      });
    } else {
      $('.navbar_section').css({
        transform: 'translateY(0rem)',
      });
      $('.button.is-icon.is-navbar').css({
        transform: 'translateY(0rem)',
      });
    }
  });
}
export { jqueryCC };
 */
